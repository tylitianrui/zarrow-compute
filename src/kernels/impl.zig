const register = @import("register.zig");

pub const compute = register.compute;
pub const registerBaseKernels = register.registerBaseKernels;
pub const registerCompatKernels = register.registerCompatKernels;
